# Changelog

## Native macOS preview - 2026-08-03

- 新增独立的 \`native/macos\` 原生工程，使用 Swift + AppKit
- 完成菜单栏常驻、Control + Command + A 全局快捷键、当前显示器截图和选区框选
- 加入窗口自动吸附、拖拽边缘贴近、像素放大镜和选区尺寸提示
- 加入矩形、椭圆、箭头、画笔、马赛克和文字标注
- 标注完成后可以重新选中、移动、缩放、改颜色和改粗细；文字可以双击再次编辑
- 颜色改用截图蒙层内置色盘，不再依赖会被截图蒙层遮挡的系统色盘；粗细使用无级滑杆并提供实时预览
- 选择标注工具后会保持当前工具，画完一个矩形、箭头、画笔或文字后可以继续画下一个
- 矩形和椭圆支持按住 Shift 等比例绘制；缩放已有标注时按住 Shift 会锁定比例
- 点击截图控制栏空白区域不会再重置选区或退出截图
- 截图和录制 UI 重新向 macOS 系统截图工具靠齐：控制栏避开 Dock、改用浅色半透明胶囊浮层、扁平图标按钮、蓝色主操作按钮、虚线选区和圆形控制点
- 修复原生截图控制栏圆角渲染不统一的问题；马赛克改成涂抹式笔刷并换成笔刷图标，不再以选框方式创建
- 修复马赛克笔在实时预览和导出时可能与涂抹轨迹错位的问题
- 马赛克笔改为明确的黑白方块遮盖效果，避免涂在文字上被原图颜色平均成一片白；工具图标改为九宫格方块
- 支持撤销、重做、删除选中标注、Enter / Command + C 复制 PNG、Command + S 保存 PNG、Esc 取消
- 加入无声区域录制，菜单栏或 Control + Command + R 可开始/停止录制，录制文件自动保存到 \`~/Movies/SnapCut\`
- 启动路径改成先注册快捷键，再延后创建菜单栏 UI；偏好设置和更新检查改为按需创建
- 加入偏好设置和检查更新入口
- 原生版先作为开发预览，不替换当前 Electron 版本

## 1.2.6 - 2026-07-30

- macOS 更新从“下载并打开 DMG”改为真正的一键全自动升级
- 下载并校验后自动退出旧版本、静默挂载 DMG、核对包标识/版本/arm64 架构并替换应用
- 安装过程先暂存新版并备份旧版，新版启动失败时自动回滚并重新打开旧版
- 更新成功后自动重新启动 SnapCut，并在设置页显示升级完成
- 增加安装助手、路径限制、参数传递、回滚结构和自动安装状态的回归测试

## 1.2.4 - 2026-07-30

- 颜色弹层改用实际 CSS 可视区域定位，修复缩放和多屏环境中的锚点偏移
- 预设颜色选中态改为紧贴色点的同心圆环，移除鼠标打开时叠加的错误焦点框
- 自定义颜色入口改为明确的彩虹图标
- 自定义颜色面板改为可拖动二维色盘和彩虹色相条，HEX 仅保留为辅助显示和精确输入
- 增加色盘、彩虹入口、圆形选中态和弹层定位的回归测试

## 1.2.3 - 2026-07-30

- 更新下载改用 Electron 的系统网络通道，兼容 Windows / macOS 的系统代理和证书环境
- 修复点击“下载并升级”时可能出现 `TypeError: fetch failed` 的问题
- 网络或证书仍受阻时显示可操作的中文提示，同时继续保持 HTTPS 证书校验
- 增加网络错误转换与更新器网络通道注入的回归测试

## 1.2.2 - 2026-07-30

- 将颜色和粗细弹层移动到页面顶层，修复缩放和多显示器下的嵌套坐标偏移
- 移除会被截图置顶窗口遮挡的系统颜色面板方案
- 新增应用内色相、饱和度、明度和 HEX 自定义颜色面板
- 增加弹层层级与自定义颜色交互的回归测试

## 1.2.0 - 2026-07-30

- 修复颜色选择弹层错位并增加视口边缘自动调整
- 修复彩色自定义颜色按钮点击无反应，选色后立即应用
- 为线条、笔刷、字号和马赛克尺寸增加实时预览
- 新增应用内检查更新、下载进度、SHA-256 校验与一键升级流程
- Windows 自动启动新版安装器，Apple Silicon Mac 自动打开新版 DMG

## 1.1.0 - 2026-07-30

- 新增窗口悬停高亮、单击吸附与拖动自由框选
- 并行执行截屏、窗口识别和覆盖层加载，减少无条件启动等待
- 修复文字输入后点击空白处丢失的问题，并保护中文输入法会话
- 标注工具独立记住颜色和尺寸，新增自定义颜色和连续尺寸滑杆
- 修复颜色样本和当前颜色预览的显示
- 取消启动时重复写入开机启动设置

## 1.0.1 - 2026-07-30

- macOS 发布目标调整为 Apple Silicon，不再发布 Intel Mac 安装包
- 产品说明统一为独立截图与标注工具定位
- Windows 与 Apple Silicon 的功能保持不变

## 1.0.0 - 2026-07-30

首个正式版本。

- Windows / macOS 托盘常驻和可修改全局快捷键
- 当前显示器冻结、区域 / 整屏截图与高 DPI 像素映射
- 矩形、椭圆、箭头、画笔、马赛克和文字标注
- 颜色、粗细、撤销、重做、复制和 PNG 保存
- 像素放大镜、物理尺寸提示、键盘操作与中文输入法处理
- macOS 屏幕录制权限引导、开机启动与本地偏好设置
- 无上传、无遥测、无截图历史
