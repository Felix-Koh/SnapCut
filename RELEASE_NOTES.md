## SnapCut v1.2.5

SnapCut 是一个快速、独立、常驻后台的 Windows / macOS 截图工具。

### 本次更新

- macOS 更新不再只下载并打开 DMG，改为真正的一键全自动流程
- 下载和 SHA-256 校验完成后，SnapCut 会自动退出旧版本、安装新版并重新启动
- 安装助手会静默挂载 DMG，并核对应用标识、版本号和 Apple Silicon 架构
- 替换前先暂存新版并备份旧版；如果新版未能稳定启动，会自动回滚并重新打开旧版
- 更新完成后设置页会直接显示当前已升级到的新版本
- 增加自动安装助手、路径限制、回滚和启动状态的回归测试

### 从 1.2.2 升级

由于 `1.2.4` 及更早版本内部还没有自动替换助手，第一次升级到 `1.2.5` 仍需从本 Release 手动安装一次。安装 `1.2.5` 后，后续版本即可在软件内完成下载、校验、替换和重启的全自动升级。

### 主要功能

- 熟悉的快捷键截图：Windows `Alt + A`，macOS `Control + Command + A`
- 区域截图、双击整屏、移动与八方向调整
- 像素放大镜和准确的高 DPI 输出尺寸
- 矩形、椭圆、箭头、画笔、马赛克、文字
- 撤销 / 重做、复制到剪贴板、保存 PNG
- 托盘 / 菜单栏常驻、可选开机启动
- 全部本机处理，不上传截图，不包含遥测

### 下载哪个文件

- Windows 10 / 11 64 位：`SnapCut-1.2.5-windows-x64.exe`
- Apple Silicon Mac（M 系列，macOS 12 或更高版本）：`SnapCut-1.2.5-macos-arm64.dmg`

ZIP 是 macOS 免挂载压缩包；一般安装优先使用 DMG。

### 未签名说明

当前 Release 尚未使用 Apple / Windows 代码签名证书，系统会显示“无法验证开发者”或“未知发布者”提醒。请只从本 GitHub Release 下载，并用 `SHA256SUMS.txt` 核对文件；不要关闭 Gatekeeper 或 SmartScreen。详细开启方法见 README。

### 已知边界

- 每次在鼠标所在的一块显示器内框选，暂不支持跨屏连续选择。
- 当系统不允许读取某个窗口边界时，仍可手动框选。
- DRM 视频、安全桌面或硬件叠加层可能无法截图。
- 暂不包含长截图、OCR、贴图、录屏和截图历史。
